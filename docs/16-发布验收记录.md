# 16 · 发布验收记录

认证 Gate：**CONDITIONAL GO**。X 本机登录、书签读取和精确 MP4 下载已通过；Snail 已生产部署，正常 Access 登录后的端到端验收仍待用户完成首次邮箱验证码交互。正式 tag/GitHub Release 由这一门禁阻止，不能只凭公开健康成功放行。

记录日期：2026-09-12。应用 0.1.0 为已通过 CI/CD 的生产候选。原始视频、浏览器产物和脱敏 HTTP 证据仅存 gitignored `.artifacts/`；本文件不含账号身份或凭据。

## 已验证

| 范围 | 真实结果 |
|---|---|
| 仓库 | https://github.com/nocoo/snail，public，MIT，main |
| 品牌 | 1.0.0；固定 Hexly SHA `352eb2652d4e11c876ef84152fc8e02f8d5ed331`；adoption `fe5f72e8a0d960a81acadc5704a04e1c4ed4f607` |
| 原始资料 | docs/01–11 原哈希不变；所选品牌 21 文件、manifest、根 logo/ICO 校验通过 |
| 本地质量 | 66 单元/Worker HTTP、12 desktop/mobile 浏览器测试通过；健康后代路径、发布认证门禁与 Basalt 实际样式均已 RED→GREEN |
| 静态与构建 | TypeScript 7.0.2、Biome 推荐规则、Vite build、Worker dry-run 通过 |
| 首次远程部署 | Worker `d0f727ba-3431-4d22-a9e1-57e42c439963`，启动测量 19ms，单自定义域 snail.hexly.ai |
| 数据与桶 | 0001/0002 远程应用成功，无待应用迁移；R2 r2.dev 关闭、无公开域名、multipart 一天回收规则已验证 |
| HTTPS 健康 | 匿名 GET `/api/live` → 200，application/json，status=ok，version=0.1.0，Cache-Control=no-store；D1/R2 探测通过 |
| 网页隔离 | `/`、`/api/me`、用户配对批准路径 → 302 至 nocoo.cloudflareaccess.com |
| 健康路径边界 | `/api/live/`、`/api/live/child` → 401 JSON `authentication_required`；Access path 匹配后代，但 Worker 精确限制公开路由 |
| 设备隔离 | POST `/api/connectors/me/heartbeat` 无凭据 → 401 JSON，没有 Access 登录重定向 |
| 发布凭据 | GitHub production Environment 独立 Cloudflare deploy Secret 已设置，无 Access 管理权限；值未落盘/输出 |
| CI/CD 固定实测 | 发布门禁实现 SHA `a4c58384e96f1d2857f6f5cd2d71a5fff1cd1987`；[CI 34701110376](https://github.com/nocoo/snail/actions/runs/34701110376)、[Deploy 34701166328](https://github.com/nocoo/snail/actions/runs/34701166328) 均 success |
| 对应自动部署版本 | Worker `e1abe78b-bf82-4baa-9484-9e095b4b35df`，启动 21ms，公开生产探针通过 |
| R2 写入/读回/删除 | 12,016,000 字节合成 MP4，SHA-256 `475079cbacedc2ed2f7d5f344074803941c0d7c5d13448e819c7e43b9c191c72`，完整解码通过，测试对象已删除；这是管理链路，未替代网页验收 |

最初代理/系统 DNS 对新域名出现 HTTP 000/TLS 或解析失败；通过真实公开 Cloudflare 地址、保持 hostname/SNI/TLS 验证复核成功。稍后原生 Bun fetch 全部匿名探针也通过。没有改 hosts、关闭证书验证或绕过 Access；不能把这次本机网络故障记为生产已宕机或凭据错误。

首次共享 Deploy 工作流 `34700481827` 在凭据步骤失败（两个 Secret 运行时为空），虽然 API 只读列名确认都已存在。直接 Environment job `34700673280` 的两个可用性布尔值为 true；Snail 自有 production job 随后真实部署成功。保留这次失败与修复，不宣称初次 CI/CD 一次通过。R2 删除测试也曾发生一次本机网络超时，随后有界重试成功。

Basalt 调整新增四个桌面/手机与亮暗主题组合用例：初次全部因应用覆盖共享颜色而失败；切换到规定的 Tailwind 三行入口、移除覆盖后，全量 12 个浏览器用例通过。检查同时包括主题与 OS 相反时的品牌选择、侧栏/页头/岛内尺寸及原有合成媒体流程。66 单元/HTTP、类型、lint、构建、Worker dry-run、研究/品牌哈希和秘密扫描通过；这次前端修改不替代下列生产认证验收。

## 真实获准 X 样例

用户指定的公开帖 `2098577796398567727`，目标媒体 `2098577761237700609`。产品 Connector 已通过本机 OpenCLI 读取 120 条有界书签窗口（两页 HTTP 200）并精准定位目标；其他书签内容和账号身份不保存。

产品下载器取得 720×1280 H.264 MP4，HTTP 200，Content-Type `video/mp4`，1,729,313 字节，时长 7.128526 秒；SHA-256 `2ee43bb5411bbae7baaa856bbb36debc01fe97d7f2b4cd288d72684d4994ea1e`。ftyp、长度、ffprobe 与 ffmpeg 完整解码通过，临时视频已删除。原版 OpenCLI 将 HTML 当 MP4 的历史失败仍保留在研究文档，产品不采用该成功标志。

这证明了真实本机身份和精确媒体下载能力；生产配对→入库→Range→播放→删除仍需单独验收。可下载的公开变体不等同原始母版或版权许可。

## 待完成的正式发布 Gate

- 用户在本机 Chrome 完成 Snail Cloudflare Access 的正常邮箱验证码登录；不要求或读取验证码、cookie、token。
- 已登录网页上传合成媒体，验证海报、编辑/搜索/收藏/批量、分片/续传/去重、真实 R2 Range 和删除。
- 真实 Connector Keychain 配对、心跳、网页批准任务、获准示例入库、撤销与晚到写入拒绝；不归档其他私人书签。
- 最终候选检查、v0.1.0 tag/GitHub Release、生产版本对应与临时对象清理。

`release:check` 要求 gitignored `.artifacts/production-acceptance.json` 记录当前 SHA/版本下七项真实操作全部 `pass`；现在没有生成这一记录，命令实测因此退出 1。缺少交互、缺项、失败或旧 SHA 均阻止正式 tag。上表固定已经完成的实现验收；后续文档提交的实时 CI/Deploy 状态以 Actions 为准，不能从旧 run 推断。尚未完成的项继续保留为待验；不会以本地合成 JWT 或空库部署替代真实生产证据。
